# Ops Fixtures

This directory stores checked-in example inputs for operational scripts.

- Keep only stable sample inputs here.
- Real run output belongs in `reports/ops/`, `reports/typecheck/`, or `tmp/`.
- Copy examples out before editing run-specific values.
