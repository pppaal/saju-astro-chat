# Global Typecheck 0-Error Plan

Current baseline is generated by:

```bash
pnpm -s ops:typecheck:metrics
```

Latest snapshot is stored in `data/ops/typecheck-metrics.json`.

Top failing files can be listed via:

```bash
pnpm -s ops:typecheck:hotspots
```

## Current breakdown

- TS7006 (implicit any): bulk cleanup candidates
- TS2305 (`@prisma/client` named exports): Prisma 7 type-export migration

## Execution order

0. **Fix highest-error hotspots first (top10 from hotspot report)**
1. **Prisma export migration (TS2305 first)**
   - Replace direct `@prisma/client` `Prisma` imports with local re-exports where available (`@/lib/db/prisma`), or align generated client typings.
2. **Implicit any cleanup (TS7006)**
   - Annotate map/filter/reduce callback params in top failing files first:
     - `src/app/api/admin/metrics/comprehensive/route.ts`
     - `src/app/api/tarot/couple-reading/route.ts`
     - `src/app/api/me/history/route.ts`
3. **Gate policy**
   - Keep running `pnpm -s ops:typecheck:metrics` in CI and fail merge when `totalErrors > 0`.

## SLO tie-in

Typecheck debt is tracked as an operational quality signal alongside runtime SLOs.
