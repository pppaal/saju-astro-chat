'use client'

import DestinyCalendar from './DestinyCalendar'

export default function CalendarTabs() {
  // Single unified calendar view only (no top mode switch / split panels)
  return <DestinyCalendar />
}
