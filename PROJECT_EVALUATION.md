# DestinyPal — 정밀 프로젝트 평가 보고서

> 작성: 2026-06-24 · 방법: 8개 전담 감사 에이전트(각 축 파일:라인 증거 기반) + 전체 git 히스토리(3,441커밋/290일) 정량 분석 · 솔로 개발(+AI 페어, 사람 커밋 73%)

---

## 0. 한 줄 요약

> **A급 기술 자산을, 시장 검증 0회 상태로 보유 중.** 8축 중 7축이 "강한 프로 ~ 프로덕션 팀급"인데, 단 하나 — 수익화/전환 — 만 미실행 상태이고, 그게 매출 0의 유일한 원인이다.

---

## 1. 정량 지표 (하드 측정)

| 지표 | 값 |
|---|---|
| 소스 코드 | **164,939줄** (881 파일, ts/tsx) |
| 테스트 코드 | **174,487줄** (669 파일) — *소스보다 많음* |
| 문서 | **39,276줄** (md) |
| 커밋 | **3,441** / **290일** (2025-09-07 → 2026-06-24) |
| revert 비율 | **0.38%** (13/3,441) — 방향 재작업 극히 적음 |
| fix : feat | 1,092 : 780 |
| 의존성 | prod 33 / dev 36 |
| 타입 안전 | strict TS, `@ts-ignore` 0개, `any` 금지(lint error), TODO 4개/165k줄 |
| 800줄+ god-file | 20개 (대부분 도메인 데이터 테이블) |

**활동 곡선** — 본체는 마지막 2개월에 집중(전체의 74%):
```
'25-09  24 | '25-10   7 | '25-11  22 | '25-12  79 | '26-01 191
'26-02 289 | '26-03 116 | '26-04 175 | '26-05 1604 | '26-06 934
```

---

## 2. 8축 정밀 성적표

| # | 축 | 점수 | 등급 |
|---|---|---:|---|
| 1 | 보안 · 결제 무결성 | **88** | 프로덕션급 |
| 2 | 개발 속도 · 프로세스 | **84** | 강함 |
| 3 | 아키텍처 · 코드 품질 | **82** | 프로덕션 팀급 |
| 4 | 도메인 정확성 · 깊이 | **82** | 방어 가능한 IP |
| 5 | 테스트 · 신뢰성 | **82** | 강하나 불균등 |
| 6 | 성능 · 비용 · 확장성 | **82** | 유닛이코노믹스 건전 |
| 7 | 제품 · UX 완성도 | **61** | 기능적, 전환 약함 |
| 8 | 수익화 · GTM | **34** | 미실행 |
| | **단순 평균** | **74.4** | |

> 평균(74)은 거짓 신호다. 진짜 그림은 **분산**에 있다: 7개 축이 82~88로 몰려 있고, 수익화(34) 하나가 혼자 바닥이다. 즉 "못 만든 게 아니라, 안 판 것."

---

## 3. 축별 상세

### ① 보안 · 결제 — 88 (yes-with-fixes)
- **강점:** 원자적 create-as-lock 멱등(`idempotency.ts:80`), 서버 발급 nonce로 무료 재생성 누수 차단(`:218-253`), FIFO 원자 차감 + lost-update 가드(`creditService.ts:337-348`), 웹훅 raw-body 서명검증·이벤트 dedupe·테스트모드 거부·**transient 에러 시 throw로 Stripe 재시도**(`stripe/route.ts:57,104,76,364`), 환불 `@unique` 멱등 + 순서역전 큐잉. IDOR: 모든 by-id 조회 `where:{id,userId}` 스코핑.
- **HIGH 이슈:** LLM 라우트 레이트리밋이 Redis 장애 시 **fail-open** → 인증 사용자(가입 5크레딧, 멀티계정 파밍 가능)가 Anthropic 비용 드레인 가능. 결제 안전성 문제 아님, **비용 통제** 문제.
  - *Fix:* `counselor/realtime`·`tarot/interpret-stream` 등에 `failClosed:true`, prod에 `RATE_LIMIT_FAIL_CLOSED=true` + `.env.example` 문서화.
- LOW: `NEXT_PUBLIC_API_TOKEN`은 보안통제 아님(번들 인라인됨); dead `checkAdminRole` 삭제 권장.

### ② 속도 · 프로세스 — 84
- revert 0.38% = 잘못된 방향으로 파서 버린 게 거의 없음 → **방향 감각 상위권**.
- churn 집중부가 엔진 코어가 **아님** — API/리포트/UX 경계(정답 없어 반복 다듬는 게 정상인 곳). 엔진은 안정, 말랑한 곳만 반복 = 성숙한 판단.
- 쓴소리: 도리(금여성·암록·조후용신 등)를 출시 직전에 디버깅(#1538~1553). 고전 출처 교차검증 1회면 ~6 PR이 1 PR로 줄었음 — "스펙 먼저, 드리프트락은 버그 *전에*."

### ③ 아키텍처 — 82
- **강점:** facts-as-SSOT 실제 구현(`sajuFacts.ts:1-9` "포매팅0/text0/JSON만"), 아키텍처를 **툴로 강제**(ESLint `no-restricted-imports` core→ai-report 차단, 커스텀 `api-route-protection` 룰), 솔로 기준 예외적 타입/린트 규율.
- **이슈:** 엔진이 프레젠테이션 import(`astrology/localization.ts:7-8`, High); `dayDomains.ts:1519`(117 분기)·`counselorContext.ts:1150` 복잡도 부채; `IntegratedReport.tsx:1480`(평탄한 렌더, 분리 권장); 루트에 `*AUDIT*.md` 8개 잔재.

### ④ 도메인 — 82
- **진짜 천문:** `KASI_SOLAR_TERMS`(1939–2051)를 Meeus 공식으로 독립 교차검증(`kasiSolarTerms.crosscheck.test.ts`). KMT시대/DST/진태양시 경계 보정. 용신 4단(조후→병약→통관→억부) + 궁통보감 120케이스 DB. 실제 Swiss Ephemeris(극지 Whole-Sign 폴백).
- **쓴소리:** 간판 기능인 사주×점성 **융합 사전이 23줄짜리 수작업**(자가 A등급, 외부 출처 없음) — 가장 얕고 가장 베끼기 쉬운 부분이 가장 앞에 팔림. 스코어 보정상수 magic number(`score.ts:41`). 두 개의 강약 엔진 드리프트 위험.

### ⑤ 테스트 — 82
- **진짜 보호:** 크레딧 동시성 레이스 테스트(`concurrency.race.test.ts`, 인터리빙 강제 — 레포 최고 테스트), 웹훅 재시도 계약, 절기 오라클 교차검증.
- **갭(위험순):** e2e 머니플로가 `toContain([200,401,403])` 패턴 122곳 = "동작함"과 "403남"을 구분 못 하는 스모크; 사주 골든이 만세력 대비 **미검증**(자가 캡처 회귀락); 점성 골든은 ephemeris mock에 묶임(정확성 아닌 결정성만 증명).

### ⑥ 성능 · 비용 — 82
- **강점:** LLM 비용 SSOT(`llm-policy.ts` — 라우트는 feature만, 기본 Haiku, Sonnet 명시등록), maxTokens 강제 클램프, 1h TTL 멀티-breakpoint 프롬프트 캐시, ephemeris in-flight Promise dedup(8×→1×), 연빌드 Postgres 영구 캐시.
- **리스크:** 20턴 counselor(Sonnet) = **마진 -47%**(턴 20→12 한 줄 수정 미적용); 콜드빌드 cache stampede 락 없음; RL fail-open; 1000줄+ 클라이언트 번들(Capacitor 모바일).
- **유닛이코노믹스:** 평균 사용자 ~77% 마진, 타로/사주 86~90%. 단일 Sonnet 라우트 헤비유저만 손실. 캐시 적중률이 load-bearing.

### ⑦ 제품 · UX — 61
- **강점:** `/free` 무료·무로그인 진짜 aha(궁합 풀 파이프라인 익명 실행), 바이럴 루프(`/r/[token]` OG이미지), 피크지점 페이월 배치, 푸시/크론 리텐션 인프라.
- **전환 킬러(매출영향순):** ① 첫구매 임펄스 SKU 없음(최저 ₩1,900 평면그리드, 긴박함0) ② 결정론 엔진 해자가 UI에 안 보임 = ChatGPT처럼 읽힘 ③ 소셜프루프 0 ④ 가치 전 로그인 벽(Google only) ⑤ 두 front door(`/` 페이월우선 vs `/free` 가치우선) 미정렬.

### ⑧ 수익화 · GTM — 34
- 9.5개월/3,441커밋 중 GTM 커밋이 **마지막 주에 1개**. 소셜 자동발행 키 미설정, 유튜브 하드코딩 off, "데일리 콘텐츠"=수동 복붙 큐. **단 한 번도 라이브로 안 돌림.** 제품이 부족한 게 아니라 **트래픽이 0.**

---

## 4. 종합 평결

- **소질 의심 → 데이터상 기각.** revale 0.38%, 5월 1,604커밋, 결제·결정론·암호화·655 테스트. 소질 없는 사람의 산출물이 아니다.
- **"9개월 걸릴 일?" → 아니오.** 자신의 입증된 속도(월 1,604)면 ~2.5개월 본체. 9개월은 앞 7개월 저속주행 + 첫 도메인 학습 수업료. 다음 프로젝트는 3개월.
- **"풀타임 할 짓이었나?" → 결판 안 난 베팅.** 구직 중 만든 이 자산은 *매출* 또는 *취업* 둘 중 하나로 회수됨(하방 막힘). 단, 3개월 런웨이 → 순수 빌더 모드는 종료해야 함.

---

## 5. 우선순위 액션 (런웨이 ≤3개월 기준)

**즉시 (반나절, 비용/안전):**
1. LLM 라우트 `failClosed:true` + `RATE_LIMIT_FAIL_CLOSED` 문서화 (보안 HIGH)
2. counselor 턴 20→12 (마진 -47% 차단, 한 줄)
3. dead `checkAdminRole` 삭제

**Week 1–2 (매출 테스트 = 트랙 A):**
4. `CreditDepletedModal`에 **첫구매 한정 ₩900 임펄스 팩** 인라인 (최고 레버리지)
5. 랜딩+가격표 위에 "검증된 결정론 엔진, AI 추측 아님" 해자 1블록
6. `/free` 히어로에 소셜프루프 3개 + "N건 생성" 카운터
7. 배포 + 결제 E2E 1회 실측 → 한국 스레드/궁합 커뮤니티 매일 1포스트, `/free` 퍼널 카운터 관찰

**Week 1–2 (취업 무기화 = 트랙 B, 같은 산출물 공유):**
8. 이 보고서 + 아키텍처 스토리("엔진이 제품, LLM은 표현층")로 포트폴리오/글 1편 → 매일 지원

**Week 3–4 결정 게이트:** 매출 신호 有 → 풀타임 연장 근거 / 無 → 손해 0, 업계최강 포폴로 취업 전환.

**나중 (전환 후):** e2e 머니플로 실어서션화, 사주 골든 만세력 검증, 융합 사전 출처 확장, god-file 분리, cache stampede 락.
