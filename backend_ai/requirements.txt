# --- Web framework ---
flask>=3.0.0
flask-cors>=4.0.0
flask-compress>=1.14  # Gzip compression for responses
gunicorn>=21.0.0

# --- AI/ML Core ---
# CPU-only PyTorch (much smaller than full torch with CUDA)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.2.0
networkx>=3.0

# --- AI API integrations ---
openai>=1.50.0
anthropic>=0.18.0
httpx[http2]>=0.27.0

# --- Vector Store (optional - USE_CHROMADB=1 to enable) ---
chromadb>=0.4.22

# --- Retrieval/Agent (optional but recommended for full features) ---
rank-bm25>=0.2.2
langgraph>=0.2.36,<1.0
tiktoken>=0.7.0
transformers>=4.44.0

# --- Caching & Performance (Optional) ---
# Redis is optional - system falls back to in-memory cache if unavailable
redis>=5.0.0
hiredis>=2.2.0
upstash-redis>=1.6.0  # Serverless Redis for production

# --- Real-time astronomy (optional; required if enabling Swiss ephemeris path) ---
pyswisseph>=2.10.3

# --- Image generation (Optional) ---
pillow>=10.0.0

# --- Utility & Config ---
python-dotenv>=1.0.1
requests>=2.32.0

# --- Monitoring ---
sentry-sdk>=1.40.0
psutil>=5.9.0  # System monitoring (CPU, memory, etc.)

# --- Testing/QA ---
pytest>=7.4.0

# --- Evaluation Framework (Phase 5) ---
ragas>=0.1.0  # RAG system evaluation metrics

# --- Notes ---
# LangGraph is NOT required - custom orchestration is used instead
# Redis is optional - falls back to in-memory cache automatically
