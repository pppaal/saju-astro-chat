import { describe, it, expect } from 'vitest';

describe('baseAllDataPrompt', () => {
  it('should generate prompts', () => {
    expect(true).toBe(true);
  });
  
  it('should format data', () => {
    expect(1).toBe(1);
  });
});
