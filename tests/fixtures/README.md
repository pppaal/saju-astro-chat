# Test Fixtures

Checked-in evaluation and sample inputs live here.

- Use this directory for deterministic datasets that tests or evaluation scripts read as input.
- Do not write generated results here.
- Generated outputs belong in `reports/`, `artifacts/`, or `tmp/` depending on whether they are curated or disposable.
